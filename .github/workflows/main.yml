name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps: 
    - uses: actions/checkout@v2
    - uses: TG908/setup-swift@v0
      with:
        version: '5.1.3'
        platform: 'ubuntu18.04'
    - run: swift build

